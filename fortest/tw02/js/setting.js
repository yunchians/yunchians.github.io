// QRCODE網址連結
function openQRcodeURL() {
    const qrcodeURL = 'https://lin.ee/jlSFMN9'; // QRCODE的網址設定
    if (qrcodeURL == '') {
        return;
    } else {
        window.open(qrcodeURL);
    }
}

// 官網網址連結
function openOfficialURL() {
    const officialURL = 'http://bb538.tc88.net/'; // 官網的網址設定
    if (officialURL == '') {
        return;
    } else {
        window.open(officialURL);
    }
}